experiment_name: random_baseline
algorithm: random
seed: 0
output_dir: runs
total_episodes: 0

env:
  num_good: 1
  num_adversaries: 3
  num_obstacles: 2
  continuous_actions: false
  max_cycles: 50
  terminate_on_success: true

evaluation:
  eval_interval: 1
  eval_episodes: 50
  seeds: [0, 1, 2, 3, 4]
